# SEM Scale Bar Agent with ERL Self‑Learning

This project implements a self‑learning agent for extracting scale bars from SEM images. It combines YOLO detection, OCR, and an LLM‑based reasoning loop with heuristic memory.

## Installation

```bash
pip install -r requirements.txt
